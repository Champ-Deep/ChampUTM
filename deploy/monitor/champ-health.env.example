# /root/champ-health.env — read by champ-health.sh. Never commit the real file.
#
# ChampBeam's authenticated system endpoint reports MaxMind credits (and anything
# else the app grades). Any cb_live_ API key works; it is read-only here.
CHAMPBEAM_API=https://share.lakeb2b.com
CHAMPBEAM_API_KEY=cb_live_xxxxxxxxxxxxxxxxxxxxxxxx
